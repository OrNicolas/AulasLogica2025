const prompt = require('prompt-sync')();

a=Number(prompt('qual numero deseja fazer a tabuada?'))
 for(let ind=1;ind<=20;ind++) {console.log('indice:'+ ind*a)}



 //sintaxe: for (variavel;ação/duração da ação; finalização) {consequência}

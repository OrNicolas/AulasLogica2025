const prompt = require('prompt-sync')();
a=Number(prompt('Qual o primeiro divisor?:'))
b=Number(prompt('Qual o segundo divisor?:'))

console.log('o resultado da divisõao de '+a+' por '+b+' eh: '+ a/b);
